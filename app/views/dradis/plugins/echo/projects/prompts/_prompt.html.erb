<%= link_to echo.project_prompt_path(current_project, prompt.id, type: @type, record: @record), class: 'btn btn-outline-primary btn-sm me-2', data: { prompt: prompt.prompt } do %>
  <i class="fa-solid <%= prompt.icon %>"></i> <%= prompt.title %>
<% end %>
