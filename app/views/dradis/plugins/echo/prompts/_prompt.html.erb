<%= link_to echo.prompt_path(prompt.id, type: @type, record: @record), class: 'btn btn-outline-secondary btn-sm', data: { prompt: prompt.prompt } do %>
  <%= prompt.icon %> <%= prompt.title %>
<% end %>&nbsp;
