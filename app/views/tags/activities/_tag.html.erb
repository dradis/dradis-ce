<% if tag %>
  <%= presenter.verb %> the
  <%= link_to tag.name, [current_project, tag] %> tag.
<% else %>
  <% if activity.action == 'destroy' %>
    deleted a tag.
  <% else %>
    <%= presenter.verb %> a tag which has since been deleted.
  <% end %>
<% end %>
