<%= form_for [@project, @tag] do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, data: {value: @tag.name} %>
  </div>
  <div class="form-group mb-0 pt-1">
    <%= submit_tag 'Update', class: 'btn btn-primary mt-4', data: { behavior: 'animate-click' } %>
  </div>
<% end %>