<%= form_tag @tag do %>
  <div class="form-group">
    <%= label_tag :name, 'Name', class: 'col-form-label' %>
  </div>
  <div class="form-group mb-0 pt-1">
    <%= submit_tag 'Create', class: 'btn btn-block btn-primary mt-4', data: { behavior: 'animate-click' } %>
  </div>
<% end %>