<section class="summary-panel">
  <h4 class="header-underline">Issues so far
    <span class="actions pt-1">
      <div class="action">
        <select class="form-select form-select-sm issues-grouping-select"
                data-controller="issues-grouping"
                data-issues-grouping-project-id-value="<%= current_project.id %>"
                data-issues-grouping-target="select"
                data-action="change->issues-grouping#change">
          <option value="tags">Tags</option>
          <optgroup label="Lists">
            <% if @list_fields.any? %>
              <% @list_fields.each do |field| %>
                <option value="list:<%= field.name %>"><%= field.name %></option>
              <% end %>
            <% else %>
              <option disabled>No lists defined in report template.</option>
            <% end %>
          </optgroup>
        </select>
      </div>
    </span>
  </h4>

  <%= turbo_frame_tag 'issues-summary',
    src: project_dashboard_issues_path(current_project),
    class: 'issues-summary-frame',
    data: { behavior: 'issues-summary' } do %>
    <%= spinner_tag %>
  <% end %>
</section>
