<% content_for :title, 'New task' %>

<%= render partial: 'poller_alerts' %>

<% content_for :breadcrumbs do %>
  <%= render partial: 'breadcrumbs' %>
<% end %>

<div id="cards_editor" class="content-container">
  <h4 class="header-underline">Add new task</h4>
  <div class="note-text-inner"> 
    <%= render 'form' %>
  </div>
</div>
