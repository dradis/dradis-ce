<% content_for :title, "#{@node.label} - add note" %>

<div class="content-container">
  <h4 class="header-underline">Add note</h4>
  <div class="note-text-inner">
    <%= render "form" %>
  </div>
</div>
