<% commentable = comment.commentable %>
<% if commentable.title? %>
  <%= presenter.verb %> on the
  <%= link_to commentable.title, [@project, commentable] %>
  <%= comment.commentable_type %>
<% else %>
  <%= presenter.verb %> on <%= link_to "#{comment.commentable_type} ##{commentable.id}", [@project, commentable] %>.
<% end %>
