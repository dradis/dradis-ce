<% commentable = comment.commentable %>
<% if commentable.title? %>
commented on the <%= "#{commentable.title} #{commentable.commentable_display_name}" %>.
<% else %>
commented on <%= "#{commentable.commentable_display_name} ##{commentable.id}" %>.
<% end %>
