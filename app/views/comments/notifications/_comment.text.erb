<% commentable = comment.commentable %>
<% if commentable.title? %>
commented on the <%= "#{commentable.title} #{commentable.class.commentable_display_name}" %>.
<% else %>
commented on <%= "#{comment.commentable_type.demodulize} ##{commentable.id}" %>.
<% end %>
