<% content_for :title, "Add new evidence to #{@node.label}" %>

<%= content_for :breadcrumb do %>
  <%= render partial: 'breadcrumbs' %>
<% end %>

<div class="content-container">
  <h4 class="header-underline">Add evidence</h4>
  <div class="note-text-inner">
    <%= render 'form' %>
  </div>
</div>
