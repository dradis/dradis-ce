spec:
  name: dradis-ce
  services:
  - dockerfile_path: Dockerfile
    git:
      branch: develop
      repo_clone_url: https://github.com/dradis/dradis-ce.git
    health_check:
      http_path: /up
    http_port: 80
    name: web
